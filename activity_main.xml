<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">
    
    <fragment android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent" android:layout_height="match_parent" />
    
    <!-- Questo è il popup a scomparsa -->
    <LinearLayout android:id="@+id/bottomSheet"
        android:layout_width="match_parent" android:layout_height="200dp"
        android:layout_gravity="bottom" android:orientation="vertical"
        android:background="#FFFFFF" android:padding="16dp"
        android:visibility="gone">
        
        <TextView android:id="@+id/tvNome" android:textSize="18sp" android:textStyle="bold"/>
        <TextView android:id="@+id/tvPrezzo" android:textSize="16sp"/>
        <Button android:id="@+id/btnVai" android:text="VAI"/>
    </LinearLayout>
</FrameLayout>
